name: new_material_widgets_example
description: Example app demonstrating the GlossyCeramic widgets.
publish_to: "none"

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  new_material_widgets:
    git:
      url: https://github.com/suryaongithub/new_material_widgets
      ref: main

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
